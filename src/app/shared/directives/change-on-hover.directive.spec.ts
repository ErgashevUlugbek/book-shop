import { ChangeOnHoverDirective } from './change-on-hover.directive';

describe('ChangeOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
