import { OpenCloseRecentlyViewedDirective } from './open-close-recently-viewed.directive';

describe('OpenCloseRecentlyViewedDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenCloseRecentlyViewedDirective();
    expect(directive).toBeTruthy();
  });
});
